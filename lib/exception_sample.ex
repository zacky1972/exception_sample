defmodule ExceptionSample do
  @moduledoc """
  This program code in Elixir is a sample handling an arithmetic error,
  which is caused by division by zero, by three methods:
  (1) without any handling, (2) by try rescue (catch), and
  (3) by Supervisor.
  """
end
